# 本地一键启动卫生间坑位预约后端
# 用法：docker compose up -d --build   然后浏览器打开 http://localhost:3000
services:
  toilet-reservation:
    build: .
    image: toilet-reservation:latest
    container_name: toilet-reservation
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped