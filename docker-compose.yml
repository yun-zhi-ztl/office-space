# 本地一键启动 OfficeSpace 办公空间管理后端
# 用法：docker compose up -d --build   然后浏览器打开 http://localhost:3000
services:
  office-space:
    build: .
    image: office-space:latest
    container_name: office-space
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped